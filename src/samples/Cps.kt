package samples

/**
 * CPS: Continuation-Passing Style (callbacks)
 *
 * Direct Style: Write actions sequentially
 *
 * Kotlin suspending functions are written in a way to allow for direct style
 *
 * Kotlin Compiler makes suspended functions callbacks behind the scenes
 *
 * What thread does a coroutine resume on?
 */
fun main() {

}

fun postItem(item: String) {

}